# MVP：V7RC 遙控載具韌體

## MVP 目標

讓 Arduino Nano 能穩定接收藍牙模組以 UART 轉送的 `SRT` 封包，以 CH1 控制左右轉向、CH2 控制前進／後退，經差速混控輸出至 M1/M2；CH3、CH4 分別控制爪子與升降舵機，通訊中斷時必須自動停止馬達。

## 範圍內

### 1. 通訊

- 預設以 D2 RX、D3 TX、9600 baud 的 `SoftwareSerial` 接收資料。
- 逐 byte 處理資料，不阻塞主迴圈。
- 辨識固定格式 `SRTdddddddddddddddd#`，其中每個 `d` 都是十進位數字。
- 支援半包、黏包、前置雜訊及錯誤後重新同步。
- 四個通道都必須位於 1000–2000，才算有效封包。

### 2. 馬達控制

- CH1 是左右轉向，CH2 是前進／後退油門；兩者必須混控後才輸出 M1/M2。
- 使用 `M1 = throttle + steering`、`M2 = throttle - steering`，結果限制於 -500 至 +500。
- 禁止將 CH1 直接輸出至 M1、CH2 直接輸出至 M2；任何馬達命令都必須取自混控結果。
- CH1/CH2 的 1500 均為中立；預設 ±25 deadband。
- CH2 的 1000 對應最大後退、2000 對應最大前進；CH1 的兩端對應最大左／右轉向。
- 混控後的左右馬達需求線性映射至 PWM 0–255。
- M1、M2 可個別設定方向反轉，供實際安裝校正。
- CH1 轉向可獨立設定反轉；預設 1000 左轉、2000 右轉。

### 3. 舵機控制

- CH3 以微秒值直接控制 D9 爪子舵機。
- CH4 以微秒值直接控制 D10 升降舵機。
- 所有值限制於已設定的安全脈寬範圍，初始位置為 1500 us。
- D11 舵機 attach 後維持可設定中立位置，等待後續通道規格。

### 4. 安全行為

- 開機及尚未收到首個有效封包時，M1/M2 PWM 均為 0。
- 連續 300 ms 未收到有效封包時停止兩路馬達。
- 無效封包不得改變任何 actuator，也不得延後 failsafe。
- 舵機在失聯時維持最後有效位置，避免機構突然跳動。
- 蜂鳴器以非阻塞方式發出短促開機提示與失聯提示；若硬體類型未確認，可先以編譯設定停用。

### 5. 可維護性

- 腳位、鮑率、deadband、timeout、反轉及舵機限制集中設定。
- 解析器與 actuator 輸出邏輯分離。
- 不使用 Arduino `String`，不在主流程使用阻塞式 `delay()`。
- 可使用共用 Arduino CLI container 為 Arduino Nano 編譯成功。

## 不在 MVP 範圍

- BLE 配對或 GATT 實作；BLE 由外接模組處理。
- V7RC APP 本身的修改。
- D11 舵機的遙控通道，因目前協定只定義四個通道。
- 電池電壓監控、遙測、自主導航、避障、速度閉迴路。
- EEPROM 校正介面、OTA 更新或手機端設定頁。
- 實機上傳及機構極限校正，除非另有明確授權與硬體資訊。

## 驗收標準

1. `SRT1500150015001500#` 使兩路 PWM 為 0，D9/D10 命令為 1500 us。
2. `SRT1500200015001500#` 使兩路馬達以設定的正向、PWM 255 運轉。
3. `SRT1500100015001500#` 使兩路馬達以設定的反向、PWM 255 運轉。
4. `SRT2000150015001500#` 使 M1 正轉、M2 反轉，載具原地右轉。
5. `SRT1000150015001500#` 使 M1 反轉、M2 正轉，載具原地左轉。
6. `SRT2000200015001500#` 使 M1 保持最大正轉、M2 停止，形成前進右轉，而不是兩顆馬達各自跟隨 CH1/CH2。
7. CH3/CH4 在 1000、1500、2000 時，D9/D10 分別收到對應微秒命令。
8. 任意非數字、錯誤前綴、錯誤結尾或超出範圍封包不改變輸出。
9. 在任意 byte 邊界拆開輸入，合併後仍只能產生一次有效更新。
10. 多個封包連續送入時，每個完整有效封包都被處理。
11. 停止有效資料超過 300 ms 後，D5/D6 PWM 歸零。
12. 韌體以 `arduino:avr:nano` 成功編譯，且不出現 SRAM 過度使用警告。

## MVP 完成定義

程式碼、設定檔與基本測試資料已提交；Arduino CLI 編譯通過；上述協定與 failsafe 案例均有可重現的驗證紀錄；README 的接線、函式庫及編譯步驟與實作一致。
