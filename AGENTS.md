# AGENTS.md

## 專案目標

本專案為 Arduino Nano 小型履帶／雙輪差速載具韌體。Arduino 透過 UART 接收藍牙模組轉送的 V7RC VPP 封包，解析 V7RC 坦克模式協定，控制兩路直流馬達、最多三顆舵機及蜂鳴器。

所有實作都應優先滿足：失聯時停止、安全上電、不中斷接收、容易調整馬達與舵機方向。

## 目標硬體與腳位

預設開發板為經典 Arduino Nano（ATmega328P，`arduino:avr:nano`）。

| 功能 | 腳位 | 說明 |
| --- | --- | --- |
| V7RC UART RX | D0/RX | 唯一的接收資料腳位，使用 Nano 硬體 `Serial` |
| UART TX | D1/TX | 不接藍牙，韌體不得由此輸出資料 |
| M1 方向 | D4 | 方向邏輯可由常數反轉 |
| M1 PWM | D5 | 速度控制 |
| M2 PWM | D6 | 速度控制 |
| M2 方向 | D7 | 方向邏輯可由常數反轉 |
| 蜂鳴器 | D8 | MVP 僅用於非阻塞狀態提示 |
| 舵機 1／爪子 | D9 | V7RC channel 3 |
| 舵機 2／上下 | D10 | V7RC channel 4 |
| 舵機 3 | D11 | 協定尚未指定，MVP 保持 1500 us 中立位置 |

V7RC 接收資料固定使用 D0/RX。D1/TX 不接線；韌體不得呼叫任何 `Serial.print()`／`Serial.write()`，避免將除錯文字送出。D0 同時連到 Nano 的 USB-UART，因此上傳或使用 Serial Monitor 時，外接接收板不可同時驅動 D0，必要時先斷開訊號線。

## V7RC 協定基線

有效封包格式固定為：

```text
SRT1500150015001500#
```

- `SRT`：坦克模式固定前綴。
- 接續四個十進位通道，每個恰好 4 位數。
- `#`：封包結尾。
- channel 1：左右轉向，必須與 channel 2 混控，不可直接對應單一馬達。
- channel 2：載具前進／後退油門，必須同時影響兩顆馬達。
- channel 3：D9 爪子舵機。
- channel 4：D10 上下舵機。
- 通道有效範圍為 1000–2000，1500 為中立；CH1 的兩端是最大左／右轉向，CH2 的兩端是最大後退／前進。

解析器必須能從雜訊、半包、連續封包重新同步。只有前綴、長度、數字欄位、終止字元及範圍全部正確時，才可更新輸出；不可使用會造成 SRAM 碎片的 Arduino `String`。

## 安全與控制規則

- `setup()` 一開始先將兩路 PWM 設為 0，再初始化其餘輸出。
- 1500 附近使用可設定 deadband，預設 ±25，避免馬達漂移。
- CH1/CH2 各自在 deadband 外正規化為 -500 至 +500，再以 `M1 = throttle + steering`、`M2 = throttle - steering` 混控並限制在 -500 至 +500。
- 預設 CH1=1000 左轉、CH1=2000 右轉；須提供獨立的轉向反轉設定。
- 混控後的左右馬達需求線性映射至 PWM 0–255；左右馬達方向須各有獨立反轉設定。
- 舵機輸入限制在 1000–2000 us，使用 `Servo.writeMicroseconds()`，不要以角度二次映射。
- 超過預設 300 ms 未收到完整有效封包時，兩路馬達必須立即停止。舵機預設維持最後有效位置，除非規格日後明確要求回中。
- 封包錯誤不可刷新 failsafe 計時器。
- 主迴圈不可使用長時間 `delay()`、阻塞式等待封包或長音效；UART 必須持續被處理。
- 馬達煞車／滑行方式取決於外部驅動板；在電路未確認前，PWM=0 為唯一保證的停止命令。

## 程式結構約定

建議 sketch 路徑為 `ArduinoNanoClawTank/ArduinoNanoClawTank.ino`，並依需要拆分：

- `config.h`：腳位、鮑率、deadband、反向、failsafe 與舵機限制。
- `v7rc_protocol.*`：逐位元組解析及封包驗證；不得直接操作馬達。
- `actuators.*`：馬達、舵機、蜂鳴器輸出與安全停止。

若維持單一 `.ino`，仍需讓解析、數值轉換、輸出及 failsafe 各自成為小函式。所有時間比較使用 `millis()` 差值寫法，以正確處理溢位。

## 函式庫與資源限制

- 使用 Arduino AVR core 的硬體 `Serial`，只接收、不傳送；不得引入 `SoftwareSerial`。
- 舵機使用官方 `Servo` library。
- 不引入動態配置、RTOS、JSON 或其他高成本通訊函式庫。
- Nano 僅有 2 KB SRAM；避免大型緩衝區，接收緩衝以單一封包附近大小為原則。

## 驗證要求

修改韌體後至少完成：

1. 以 Arduino CLI Docker container 對 `arduino:avr:nano:cpu=atmega328old` 編譯；若實際板為新版 bootloader，再驗證 `cpu=atmega328`。
2. 驗證中立、全前進、全後退、原地左／右轉、油門加轉向、轉向反轉及 deadband 邊界。
3. 驗證半包、錯誤前綴、非數字、超範圍、缺少 `#`、雜訊及連續封包。
4. 驗證停止傳送後 300 ms 內 PWM 歸零，且無效封包不能延後停止。

除非使用者明確要求，不進行實機上傳；編譯與靜態驗證可以直接執行。

## 尚待硬體確認

- Nano 是 ATmega328P 經典版、Every，或相容板；bootloader 為新或舊。
- 藍牙接收板型號、UART 鮑率與 D0 輸入邏輯電壓。
- M1/M2 的正向電位，以及左右馬達是否需反轉。
- 三顆舵機的機構用途、允許行程及 D11 控制來源。
- 蜂鳴器是主動式或被動式。
